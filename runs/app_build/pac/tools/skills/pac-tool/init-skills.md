# tool init-skills

Extract PAC CLI AI skill definition files to a local folder.

## Arguments

- `--output` (`-o`) - Output folder for skills files. Default: .github/skills
- `--skills` (`-s`) - Comma-separated list of skill names (top-level noun names) to copy, e.g. admin,auth. Default: all skills.

