# copilot pull

Pull remote changes from Copilot Studio and merge with local workspace.

## Arguments

- `--project-dir` (`-pd`) - Path to the project directory. Defaults to the current working directory.

