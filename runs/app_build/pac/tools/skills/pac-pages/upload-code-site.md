# pages upload-code-site

Uploads your compiled code to Power Pages site

## Arguments

- `--rootPath` (`-rp`) - Root source folder to initialize Power Pages code
- `--compiledPath` (`-cp`) - Location of compiled code
- `--siteName` (`-sn`) - Name of the site

