# code list-connection-references

Lists connection references in the specified environment and solution.

## Arguments

- `--solutionId` (`-s`) - The ID of the solution to list connection references from.
- `--environment` (`-env`) - The environment URL to list connection references from. If not provided, uses the current environment.

