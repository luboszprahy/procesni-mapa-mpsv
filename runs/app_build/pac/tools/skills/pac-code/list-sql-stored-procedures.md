# code list-sql-stored-procedures

Lists stored procedures accessible by the provided SQL connection and dataset.

## Arguments

- `--connectionId` (`-c`) - The connection id of the datasource.
- `--dataset` (`-d`) - The dataset name of the datasource.

