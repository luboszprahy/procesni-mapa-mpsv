# code delete-data-source

Deletes a data source from the current app.

## Arguments

- `--dataSourceName` (`-ds`) - The name of the data source to delete.
- `--apiId` (`-a`) - The api id of the datasource.
- `--storedProcedure` (`-sp`) - The stored procedure name to remove from the app.

