# code list-datasets

Lists datasets accessible by the provided connector.

## Arguments

- `--apiId` (`-a`) - The api id of the datasource.
- `--connectionId` (`-c`) - The connection id of the datasource.

