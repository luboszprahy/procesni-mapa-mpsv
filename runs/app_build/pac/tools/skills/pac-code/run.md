# code run

Runs a local server for connections loading locally in the app.

## Arguments

- `--port` (`-p`) - The port to run the local server on.
- `--appUrl` (`-a`) - The local url the app is running on.

