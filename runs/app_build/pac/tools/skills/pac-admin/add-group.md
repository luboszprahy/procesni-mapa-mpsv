# admin add-group

Add environment to a group.

## Arguments

- `--environment-group` (`-eg`) - Environment group.
- `--environment` (`-env`) - Environment (ID, org ID, url, unique name, or partial name).

