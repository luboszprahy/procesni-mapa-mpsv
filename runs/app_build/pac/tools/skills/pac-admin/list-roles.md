# admin list-roles

List security roles from a Dataverse environment.

## Arguments

- `--environment` (`-env`) - ID or URL of the environment to list security roles from.
- `--business-unit` (`-bu`) - Name or ID of business unit to filter security roles by.
- `--include-child-roles` - Include child security roles. By default, only parent security roles are listed.

